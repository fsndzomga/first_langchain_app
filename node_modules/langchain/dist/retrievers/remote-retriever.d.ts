import { Document } from "../document.js";
import { BaseRetriever } from "../schema/index.js";
import { AsyncCaller, AsyncCallerParams } from "../util/async_caller.js";
interface RemoteRetrieverParams extends AsyncCallerParams {
    url: string;
    auth?: Record<string, string>;
    inputKey?: string;
    responseKey?: string;
    pageContentKey?: string;
    metadataKey?: string;
}
export declare class RemoteLangChainRetriever extends BaseRetriever {
    url: string;
    auth?: Record<string, string>;
    inputKey: string;
    responseKey: string;
    asyncCaller: AsyncCaller;
    pageContentKey: string;
    metadataKey: string;
    constructor({ url, auth, inputKey, responseKey, pageContentKey, metadataKey, ...rest }: RemoteRetrieverParams);
    getRelevantDocuments(query: string): Promise<Document[]>;
}
export {};
